import PageSkeleton from "@/app/components/skeletons/PageSkeleton";

/**
 * Loading state for about page.
 */
export default function Loading() {
  return <PageSkeleton hasTitle maxWidth={1200} />;
}
